$('main.container').html("<%=j render 'show' %>");
history.pushState({turbolinks: true, url: '<%= country_path(@country) %>'}, null, '<%= country_path(@country) %>');
document.title = '<%= title @title %>';
